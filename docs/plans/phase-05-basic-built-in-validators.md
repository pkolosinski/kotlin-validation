# Phase 5: Basic Built-In Validators

## Status

Completed: 0 / 7 tasks, 0%.

## Tasks

| Status | ID | Task | Files likely touched | Done when |
|---|---|---|---|---|
| Pending | P5-001 | Implement `notNull`. | Validators file, tests | Null values fail with code `not_null`. |
| Pending | P5-002 | Implement `notBlank`. | Validators file, tests | Null or blank strings fail with code `not_blank`. |
| Pending | P5-003 | Implement `minLength`. | Validators file, tests | Short strings fail with code `min_length`. |
| Pending | P5-004 | Implement `maxLength`. | Validators file, tests | Long strings fail with code `max_length`. |
| Pending | P5-005 | Implement string `regex`. | Validators file, tests | Non-matching strings fail with code `regex`. |
| Pending | P5-006 | Implement `email`. | Validators file, tests | Invalid email strings fail with code `email`. |
| Pending | P5-007 | Implement `url`. | Validators file, tests | Invalid URL strings fail with code `url` using KMP-safe logic. |

